fn main() {
    // type de variable :
    // u8 u16 u32 u64 u128
    // u = unsigned (entier non signé)
    // u8 (0->256)
    // place en mémoire pour cette variable
    // i8 i16 i32 i64 i128
    // i = integer (entier signé)
    // usize & isize
    let ma_variable: u8 = 12;
    println!("Ma variable est egale à {}", ma_variable);
}
